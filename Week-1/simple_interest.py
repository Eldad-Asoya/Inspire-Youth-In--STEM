#write a program to calculate principal interest
p = input("Enter the principal amount")
r = input("Enter the rate:")
T = input("Enter the time duration")
simple_interest = (int(p)*int(r)*int(T))/100
print("simple interest")